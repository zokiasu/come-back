SUPABASE_URL=
SUPABASE_ANON_KEY=
NUXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
YOUTUBE_API_KEY=

# Web Push (VAPID) — générer avec : node -e "const wp=require('web-push'); console.log(JSON.stringify(wp.generateVAPIDKeys(),null,2))"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@come-back.app

# Secret pour les endpoints cron (ex: openssl rand -hex 32)
CRON_SECRET=
